# BishopForge Backend Configuration
# Copy this file to .env and customize for your environment

# ============================================
# ENGINE CONFIGURATION
# ============================================

# Engine mode: "python" (lightweight), "simpleengine" (Stockfish), or "external" (remote)
BF_ENGINE_MODE=python

# Path to Stockfish binary (required if BF_ENGINE_MODE=simpleengine)
# Linux/Docker: /usr/games/stockfish
# macOS (Homebrew): /opt/homebrew/bin/stockfish
# Windows: C:\path\to\stockfish.exe
BF_STOCKFISH_PATH=

# External engine URL (required if BF_ENGINE_MODE=external)
BF_EXTERNAL_ENGINE_URL=

# Number of engine instances in the pool (default: 1)
# Increase for production to handle concurrent requests
BF_ENGINE_POOL_SIZE=1

# ============================================
# CACHING
# ============================================

# Enable in-memory caching for analysis results (true/false)
BF_CACHE_ENABLED=false

# ============================================
# CORS & SECURITY
# ============================================

# Allow CORS (true/false)
# Set to true for development, restrict origins in production
BF_ALLOW_CORS=true

# ============================================
# LOGGING
# ============================================

# Log level: debug, info, warning, error, critical
BF_LOG_LEVEL=info

# ============================================
# DEPLOYMENT EXAMPLES
# ============================================

# Development (lightweight, no Stockfish required):
# BF_ENGINE_MODE=python
# BF_CACHE_ENABLED=false
# BF_ALLOW_CORS=true
# BF_LOG_LEVEL=debug

# Production (Stockfish on Fly.io/Railway):
# BF_ENGINE_MODE=simpleengine
# BF_STOCKFISH_PATH=/usr/games/stockfish
# BF_ENGINE_POOL_SIZE=4
# BF_CACHE_ENABLED=true
# BF_ALLOW_CORS=true
# BF_LOG_LEVEL=info

